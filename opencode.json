{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "magic": {
      "type": "local",
      "command": ["npx", "-y", "@21st-dev/magic@latest", "mcp"],
      "enabled": true,
      "env": {
        "API_KEY": "{env:MAGIC_MCP_API_KEY}"
      }
    }
  }
}
